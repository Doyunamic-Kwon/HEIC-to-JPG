# HEIC to JPG Converter for macOS

macOS 사용자를 위한 초경량, 고성능 HEIC to JPG 변환기입니다.
Python으로 시작하여 Swift Native 앱으로 진화한 프로젝트입니다.

## 🤔 HEIC vs JPG: 왜 변환해야 할까요?

이 프로젝트는 **"왜 아이폰 사진은 컴퓨터에서 바로 안 열릴까?"** 라는 의문에서 시작되었습니다. 두 포맷은 저마다의 강력한 특징이 있습니다.

### 🍏 HEIC (High Efficiency Image Container)
HEIC는 MPEG에서 개발한 최신 이미지 포맷으로, Apple 기기의 표준입니다.
*   **압축 효율의 왕**: JPG 대비 **절반의 용량**으로 동일한 화질을 보여줍니다.
*   **고품질 색상**: JPG는 8비트 색상만 지원하지만, HEIC는 **16비트**를 지원하여 풍부한 색감을 담습니다.
*   **다기능**: '라이브 포토'처럼 연사 이미지나 깊이 정보(Depth map)를 하나의 파일에 담을 수 있습니다.
*   **단점**: **호환성**이 치명적입니다. Windows 구형 버전이나 대부분의 웹사이트에서는 바로 업로드하거나 볼 수 없습니다.

### 📷 JPG (JPEG)
1992년에 탄생한 가장 대중적인 이미지 포맷입니다.
*   **압도적인 호환성**: 냉장고부터 최신 컴퓨터까지, 지구상 거의 모든 전자기기에서 열립니다.
*   **웹 표준**: 웹사이트 업로드 시 가장 안전한 선택입니다.
*   **단점**: 압축 시 화질 저하가 발생하며(블록 노이즈), 용량이 HEIC보다 큽니다.

### 🚀 결론
*   **촬영/보관**은 용량을 아껴주는 **HEIC**가 유리합니다.
*   **공유/업로드**는 어디서든 열리는 **JPG**가 필수적입니다.
**이 프로그램은 보관된 추억(HEIC)을 세상과 공유(JPG)하기 위한 가교 역할을 합니다.**

---

## 🛠️ 기술 분석: 코드와 함께 보는 구현 차이

이 프로젝트는 두 가지 다른 접근 방식으로 HEIC 변환을 시도했습니다. 각 코드가 어떻게 동작하는지 살펴보면 기술적 차이를 명확히 알 수 있습니다.

### 1. Python (`heic_to_jpg_converter.py`)
초기 버전은 Python의 강력한 라이브러리 생태계를 활용했습니다.

```python
# heic_to_jpg_converter.py

# 1. 외부 라이브러리 의존성
from PIL import Image
from pillow_heif import register_heif_opener

# 2. HEIC 포맷 등록 (기본 지원 X)
register_heif_opener()

# 3. 변환 로직
image = Image.open(file_path)
image.convert("RGB").save(save_path, "JPEG", quality=95)
```
*   **분석**:
    *   **의존성**: Python은 기본적으로 HEIC를 읽을 수 없어 `pillow-heif`라는 외부 C 라이브러리 바인딩이 필요합니다.
    *   **색상 공간 변환 (`convert("RGB")`)**: JPG는 투명도(Alpha 채널)를 지원하지 않으므로, HEIC의 RGBA 데이터를 강제로 RGB로 변환해야 합니다. 이 과정에서 일부 데이터 손실이 발생할 수 있습니다.
    *   **한계**: OS 버전에 따라 `tkinter` GUI 라이브러리가 충돌하는 문제가 발생했습니다 (macOS 호환성 이슈).

### 2. Swift (`HEICConverter.swift`)
최종 버전은 macOS의 내장 프레임워크를 직접 호출합니다.

```swift
// HEICConverter.swift

import ImageIO // macOS 이미지 처리 코어 프레임워크

private func convertHEICtoJPG(url: URL) -> Bool {
    // 1. 소스 생성 (별도 라이브러리 없이 바로 읽기)
    guard let source = CGImageSourceCreateWithURL(url as CFURL, nil) else { return false }
    
    // 2. 목적지 설정 (JPG 포맷 지정)
    guard let destination = CGImageDestinationCreateWithURL(newURL as CFURL, UTType.jpeg.identifier as CFString, 1, nil) else { return false }
    
    // 3. 압축 옵션 설정
    let options: [CFString: CGFloat] = [kCGImageDestinationLossyCompressionQuality: 0.95]
    
    // 4. 변환 및 저장
    CGImageDestinationAddImageFromSource(destination, source, 0, options as CFDictionary)
    return CGImageDestinationFinalize(destination)
}
```
*   **분석**:
    *   **Native Performance**: `ImageIO`는 macOS 시스템 레벨에서 최적화된 프레임워크입니다. Python처럼 인터프리터를 거치지 않고 하드웨어 가속을 직접 사용합니다.
    *   **무설치**: 별도의 라이브러리 설치(`pip install ...`)가 필요 없습니다.
    *   **타입 안전성**: `UTType.jpeg`와 같이 명확한 타입을 사용하여 문자열 오타로 인한 오류를 방지합니다.

---

## 🚀 왜 Swift로 전환했나요?

| 특징 | Python (Pillow) | Swift (ImageIO) |
| :--- | :--- | :--- |
| **속도** | 느림 (인터프리터 방언) | **매우 빠름 (네이티브)** |
| **용량** | 수십 MB (런타임 포함) | **수십 KB (단일바이너리)** |
| **호환성** | 불안정 (OS 업데이트에 민감) | **완벽함 (Apple 공식 API)** |
| **HEIC 처리** | 외부 라이브러리 필수 | **OS 내장 기능 활용** |

결국 **"사용자가 편하게 쓸 수 있는 도구"**가 되려면 설치 과정이 없어야 하고, OS와 물아일체로 동작해야 한다는 결론에 도달하여 Swift로 코드를 완전히 재작성했습니다.

---

## 💻 사용 방법

### 앱 실행
1. 빌드된 `HEIC Converter` 앱을 실행합니다.
2. 변환하고 싶은 HEIC 파일들을 앱 창으로 **드래그 앤 드롭** 하세요.
3. **[변환 시작]** 버튼을 누르면 원본 폴더에 JPG 파일이 생성됩니다.

### 직접 빌드하기 (터미널)
소스 코드를 직접 빌드하여 앱을 생성할 수 있습니다.

```bash
# 1. 소스 코드 컴파일
swiftc HEICConverter.swift -o HEIC_Convert_App -framework SwiftUI -framework ApplicationServices -framework UniformTypeIdentifiers -framework ImageIO -parse-as-library

# 2. 앱 패키지 폴더 생성 및 실행 파일 이동
mkdir -p "HEIC Converter.app/Contents/MacOS"
mv HEIC_Convert_App "HEIC Converter.app/Contents/MacOS/HEIC Converter"

# 3. Info.plist 설정 (필수 정보 입력) -> 저장소의 Info.plist 파일 참조
cp Info.plist "HEIC Converter.app/Contents/"
```

## 📝 License
MIT License
